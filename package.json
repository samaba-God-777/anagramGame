{
  "name": "english-learning-hub",
  "version": "2.0.0",
  "description": "Interactive English grammar learning platform with games and exercises",
  "type": "module",
  "scripts": {
    "dev": "cd client && vite",
    "server": "node server/server.js",
    "start": "node server/server.js",
    "build": "cd client && vite build",
    "preview": "cd client && vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "dotenv": "^16.4.0",
    "express": "^4.18.0",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
